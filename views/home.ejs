<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Samarpan</title>
    <link rel="icon" href="./Pics/top-logo.jpeg.jpg" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/home.css" />
  </head>
  <body>
    <%- include("includes/navbar.ejs"); %>

    <div class="main-body">
      <div class="p-1">
        <div class="w-note">
          <p>
            Welcome to Samarpan <br />
            A Step Taken To Backbone Local Start-Ups
          </p>
        </div>

        <div class="slide-show"><img src="/images/home-pic.jpg" /></div>
      </div>

      <h2 class="division-heading"><u>Jammu Division</u></h2>

      <div class="cards-container">
        <% for (let district of jammu) {%>

        <div onclick="location.href='/<%= district.name %>'" class="card">
          <%= district.name %>
        </div>
        <% } %>
      </div>

      <h2 class="division-heading"><u>Kashmir Division</u></h2>

      <div class="cards-container">
        <% for (let district of kashmir) {%>

        <div onclick="location.href='/<%= district.name %>'" class="card">
          <%= district.name %>
        </div>
        <% } %>
      </div>
    </div>

    <%- include("includes/footer.ejs"); %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
